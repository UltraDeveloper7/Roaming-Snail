#version 330 core
in vec2 TexCoords;
out vec4 color;

uniform sampler2D text;
uniform vec3 textColor;
uniform float alphaMul; //multiplies glyph alpha (e.g., for shadow pass)


void main()
{
    float a = texture(text, TexCoords).r * alphaMul;
    color = vec4(textColor, a);
}